export class criança{ //podemos utilizar o método export para podermos utilizar este objeto e seus métodos em outros lugares.
    public correr(){
        console.log("Está correndo!")
    }
    public pular(){
        console.log("Está pulando!")
    }
    public andar(){
        console.log("Está andando!");
    }
}

// Aqui nós temos, de forma simples, o mesmo objeto criado, dessa vez em forma de classe, portando todos os métodos necessários para que possamos usar posteriormente
